# Archived Experiment 1 validation summary

`validation-summary.csv` is copied from the supplied July 2026 ESNet validation
artifact and is kept only as a regression reference for reruns.

The supplied archive's largest delay-adjusted 1 s NMAE is **7.2051%**
(run 7, flow 7002). This differs from the **6.60%** maximum currently written
in the experiment-plan document. The rerun harness follows the supplied ESNet
artifact exactly and does not alter the validation calculation to force the
6.60% value. Reconcile that manuscript/document number against the rerun before
final submission.
